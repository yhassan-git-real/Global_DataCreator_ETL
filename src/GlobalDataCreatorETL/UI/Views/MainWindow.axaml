<Window xmlns="https://github.com/avaloniaui"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       xmlns:controls="using:GlobalDataCreatorETL.UI.Views.Controls"
       xmlns:vm="using:GlobalDataCreatorETL.UI.ViewModels"
       x:Class="GlobalDataCreatorETL.UI.Views.MainWindow"
       Title="Global DataCreator ETL"
       Width="1280" Height="820"
       MinWidth="960" MinHeight="640"
       WindowStartupLocation="CenterScreen"
       Background="#F1F5F9">

    <Grid RowDefinitions="Auto,0,*,Auto">

        <!-- ═══ Row 0: Application Header ═══════════════════════════════════ -->
        <Border Grid.Row="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Color="#0F172A" Offset="0" />
                    <GradientStop Color="#1E3A5F" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid ColumnDefinitions="*,Auto" Margin="16 9">
                <!-- Left: App identity -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="⚡" FontSize="20" VerticalAlignment="Center" Foreground="White" />
                    <StackPanel VerticalAlignment="Center" Spacing="1">
                        <TextBlock Text="Global DataCreator ETL"
                                   FontSize="15" FontWeight="Bold" Foreground="White" />
                        <TextBlock Text="Trade Data Extraction &amp; Excel Generation"
                                   FontSize="10" Foreground="#94A3B8" />
                    </StackPanel>
                </StackPanel>
                <!-- Right: Live connection info -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8"
                            VerticalAlignment="Center">
                    <TextBlock FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center"
                               Text="{Binding ConnectionInfo.ServerName}" />
                    <TextBlock FontSize="11" Foreground="#334155" VerticalAlignment="Center" Text="|" />
                    <TextBlock FontSize="11" Foreground="#94A3B8" VerticalAlignment="Center"
                               Text="{Binding ConnectionInfo.DatabaseName}" />
                    <TextBlock FontSize="11" Foreground="#334155" VerticalAlignment="Center" Text="|" />
                    <StackPanel Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
                        <Ellipse Width="7" Height="7" VerticalAlignment="Center"
                                 Fill="{Binding ConnectionInfo.StatusColor}" />
                        <TextBlock FontSize="11" FontWeight="SemiBold" Foreground="White"
                                   VerticalAlignment="Center"
                                   Text="{Binding ConnectionInfo.StatusMessage}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══ Row 1: Menu Bar (hidden) ══════════════════════════════════ -->

        <!-- ═══ Row 2: Main Content ══════════════════════════════════════════ -->
        <Grid Grid.Row="2" RowDefinitions="Auto,*" Margin="20 14">

            <!-- ── Group 1: Filter Parameters ───────────────────────── -->
            <Border Grid.Row="0" Classes="Card" Margin="0 0 0 12">
                <StackPanel Spacing="0">
                    <TextBlock Classes="CardHeader" Text="Filter Parameters" />
                    <controls:InputFieldsControl />
                </StackPanel>
            </Border>

            <!-- ── Group 2: Control, Output &amp; Status ────────────── -->
            <controls:ControlsPanelControl Grid.Row="1" />

        </Grid>

        <!-- ═══ Row 3: Footer ════════════════════════════════════════════════ -->
        <controls:FooterControl Grid.Row="3" />

    </Grid>

</Window>
