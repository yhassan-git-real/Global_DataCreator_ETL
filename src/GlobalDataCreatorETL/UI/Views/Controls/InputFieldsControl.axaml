<UserControl xmlns="https://github.com/avaloniaui"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:converters="using:GlobalDataCreatorETL.UI.Converters"
            xmlns:helpers="using:GlobalDataCreatorETL.UI.Helpers"
            x:Class="GlobalDataCreatorETL.UI.Views.Controls.InputFieldsControl">

    <UserControl.Resources>
        <converters:MonthIndexConverter x:Key="MonthIndex" />
    </UserControl.Resources>

    <StackPanel Spacing="0">

        <!-- ── Row 1: Date From | Date To | HS Code ────────────────────── -->
        <Grid ColumnDefinitions="*,8,*,8,*" Margin="0 0 0 10">

            <StackPanel Grid.Column="0" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Date From" />
                <Grid ColumnDefinitions="*,4,64">
                    <ComboBox Grid.Column="0" Classes="FormComboBox"
                              SelectedIndex="{Binding Filter.FromMonth, Converter={StaticResource MonthIndex}}"
                              ItemsSource="{x:Static helpers:DateHelpers.MonthNames}" />
                    <TextBox Grid.Column="2" Classes="FormInput"
                             Text="{Binding Filter.FromYear}"
                             Watermark="Year" />
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="2" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Date To" />
                <Grid ColumnDefinitions="*,4,64">
                    <ComboBox Grid.Column="0" Classes="FormComboBox"
                              SelectedIndex="{Binding Filter.ToMonth, Converter={StaticResource MonthIndex}}"
                              ItemsSource="{x:Static helpers:DateHelpers.MonthNames}" />
                    <TextBox Grid.Column="2" Classes="FormInput"
                             Text="{Binding Filter.ToYear}"
                             Watermark="Year" />
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="4" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="HS Code" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.HsCode}"
                         Watermark="e.g. 20, 30, 40 — comma-separated or blank for all" />
            </StackPanel>

        </Grid>

        <!-- ── Row divider ──────────────────────────────────────────────── -->
        <Border Height="1" Margin="0 0 0 10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Color="#1E3050" Offset="0" />
                    <GradientStop Color="#172840" Offset="0.5" />
                    <GradientStop Color="#101A28" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- ── Row 2: Product | IEC Code | Company ──────────────────────── -->
        <Grid ColumnDefinitions="*,8,*,8,*" Margin="0 0 0 10">

            <StackPanel Grid.Column="0" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Product" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.Product}"
                         Watermark="e.g. Iron, Steel, Alum — comma-separated" />
            </StackPanel>

            <StackPanel Grid.Column="2" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="IEC Code" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.IecCode}"
                         Watermark="e.g. IEC001, IEC002 — comma-separated" />
            </StackPanel>

            <StackPanel Grid.Column="4" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="{Binding CompanyLabel}" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.CompanyName}"
                         Watermark="e.g. ABC Corp, XYZ Ltd — comma-separated" />
            </StackPanel>

        </Grid>

        <!-- ── Row divider ──────────────────────────────────────────────── -->
        <Border Height="1" Margin="0 0 0 10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Color="#1E3050" Offset="0" />
                    <GradientStop Color="#172840" Offset="0.5" />
                    <GradientStop Color="#101A28" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- ── Row 3: Foreign Country Code | Foreign Name | Port ─────────── -->
        <Grid ColumnDefinitions="*,8,*,8,*" Margin="0 0 0 10">

            <StackPanel Grid.Column="0" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Foreign Country Code" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.ForeignCountryCode}"
                         Watermark="e.g. USA, CHN — comma-separated" />
            </StackPanel>

            <StackPanel Grid.Column="2" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Foreign Name" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.ForeignName}"
                         Watermark="e.g. Party A, Party B — comma-separated" />
            </StackPanel>

            <StackPanel Grid.Column="4" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Port" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.Port}"
                         Watermark="e.g. INNSA, INMAA — comma-separated" />
            </StackPanel>

        </Grid>

        <!-- ── Row divider ──────────────────────────────────────────────── -->
        <Border Height="1" Margin="0 0 0 10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0%,0%" EndPoint="100%,0%">
                    <GradientStop Color="#1E3050" Offset="0" />
                    <GradientStop Color="#172840" Offset="0.5" />
                    <GradientStop Color="#101A28" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- ── Row 4: Country | Excel File Name | Output Directory ─────── -->
        <Grid ColumnDefinitions="*,8,*,8,*">

            <StackPanel Grid.Column="0" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Country" />
                <ComboBox Classes="FormComboBox"
                          ItemsSource="{Binding Countries}"
                          SelectedItem="{Binding SelectedCountry}"
                          DisplayMemberBinding="{Binding Name}"
                          PlaceholderText="Select country…" />
            </StackPanel>

            <StackPanel Grid.Column="2" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Excel File Name" />
                <TextBox Classes="FormInput"
                         Text="{Binding Filter.UserFileName}"
                         Watermark="Auto-generated per combination (single run only)" />
            </StackPanel>

            <StackPanel Grid.Column="4" Spacing="5">
                <TextBlock Classes="FieldLabel" Text="Output Directory" />
                <Grid ColumnDefinitions="*,4,Auto">
                    <TextBox Grid.Column="0" Classes="FormInput"
                             Text="{Binding Filter.OutputDirectory}"
                             Watermark="Select output folder…" />
                    <Button Grid.Column="2" Classes="OutlineButton"
                            Command="{Binding BrowseFolderCommand}"
                            Content="Browse" />
                </Grid>
            </StackPanel>

        </Grid>

        <!-- ── Validation message ───────────────────────────────────────── -->
        <TextBlock Classes="ValidationMessage"
                   Text="{Binding ValidationMessage}"
                   IsVisible="{Binding HasValidationError}"
                   Margin="0 8 0 0" />

    </StackPanel>

</UserControl>
